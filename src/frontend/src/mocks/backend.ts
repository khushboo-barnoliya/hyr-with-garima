import type { backendInterface, BookingStatus, GoalType, UserRole } from "../backend";
import { ExternalBlob } from "../backend";
import { Principal } from "@icp-sdk/core/principal";

const mockPrincipal = Principal.fromText("aaaaa-aa");
const now = BigInt(Date.now()) * BigInt(1_000_000);
const day = BigInt(86_400_000_000_000);

// Helper for ExternalBlob-like object
const makeBlob = (url: string): ExternalBlob => ExternalBlob.fromURL(url);

export const mockBackend: backendInterface = {
  adminAssignMealPlanToMember: async () => {},
  assignCallerUserRole: async () => {},
  assignMealPlanToMember: async () => {},
  assignPlanToMember: async () => {},
  bookSession: async () => ({
    id: BigInt(1),
    status: "confirmed" as unknown as BookingStatus,
    memberId: mockPrincipal,
    bookedAt: now,
    sessionId: BigInt(1),
  }),
  cancelBooking: async () => {},
  createFitnessPlan: async (name, description, durationDays, price) => ({
    id: BigInt(1),
    name,
    description,
    durationDays,
    price,
    createdAt: now,
  }),
  createMealPlan: async (name, description, goalType, dailyCalories, proteinGrams, carbsGrams, fatGrams) => ({
    id: BigInt(1),
    name,
    description,
    goalType: goalType as unknown as GoalType,
    dailyCalories,
    proteinGrams,
    carbsGrams,
    fatGrams,
    createdAt: now,
  }),
  createRecipeGuide: async (title, description, goalType, file, filename) => ({
    id: BigInt(1),
    title,
    description,
    goalType: goalType as unknown as GoalType,
    file,
    filename,
    createdAt: now,
  }),
  createSession: async (title, description, startTime, endTime, capacity) => ({
    id: BigInt(1),
    title,
    description,
    startTime,
    endTime,
    capacity,
    bookedCount: BigInt(0),
  }),
  createTestimonial: async (clientName, content, rating) => ({
    id: BigInt(1),
    clientName,
    content,
    rating,
    createdAt: now,
  }),
  createTransformation: async (clientName, description, beforeImage, afterImage) => ({
    id: BigInt(1),
    clientName,
    description,
    beforeImage,
    afterImage,
    createdAt: now,
  }),
  deleteFitnessPlan: async () => {},
  deleteMealPlan: async () => {},
  deleteMeasurement: async () => {},
  deleteRecipeGuide: async () => {},
  deleteSession: async () => {},
  deleteTestimonial: async () => {},
  deleteTransformation: async () => {},
  getAllPlanAssignments: async () => [
    {
      memberId: mockPrincipal,
      planId: BigInt(1),
      assignedAt: now - day * BigInt(30),
      expiresAt: now + day * BigInt(60),
    },
  ],
  getCallerUserProfile: async () => ({ name: "Alex Johnson" }),
  getCallerUserRole: async () => "user" as unknown as UserRole,
  getFitnessPlan: async () => ({
    id: BigInt(1),
    name: "12-Week Body Transformation",
    description: "Complete body recomposition program with progressive overload.",
    durationDays: BigInt(84),
    price: 299,
    createdAt: now,
  }),
  getMealPlan: async () => ({
    id: BigInt(1),
    name: "High Protein Fat Loss",
    description: "Calorie deficit plan focused on preserving muscle.",
    goalType: "weightLoss" as unknown as GoalType,
    dailyCalories: BigInt(1800),
    proteinGrams: BigInt(180),
    carbsGrams: BigInt(150),
    fatGrams: BigInt(60),
    createdAt: now,
  }),
  getMyBookings: async () => [
    {
      id: BigInt(1),
      status: "confirmed" as unknown as BookingStatus,
      memberId: mockPrincipal,
      bookedAt: now - day,
      sessionId: BigInt(1),
    },
  ],
  getMyMacroLogs: async () => [
    {
      id: BigInt(1),
      memberId: mockPrincipal,
      date: now - day,
      calories: BigInt(1850),
      proteinGrams: BigInt(175),
      carbsGrams: BigInt(145),
      fatGrams: BigInt(65),
      notes: "Post-workout meal included",
      loggedAt: now,
    },
    {
      id: BigInt(2),
      memberId: mockPrincipal,
      date: now - day * BigInt(2),
      calories: BigInt(1790),
      proteinGrams: BigInt(180),
      carbsGrams: BigInt(130),
      fatGrams: BigInt(58),
      notes: "",
      loggedAt: now - day,
    },
  ],
  getMyMealPlan: async () => ({
    id: BigInt(1),
    name: "High Protein Fat Loss",
    description: "Calorie deficit plan focused on preserving muscle.",
    goalType: "weightLoss" as unknown as GoalType,
    dailyCalories: BigInt(1800),
    proteinGrams: BigInt(180),
    carbsGrams: BigInt(150),
    fatGrams: BigInt(60),
    createdAt: now,
  }),
  getMyMeasurements: async () => [
    {
      id: BigInt(1),
      memberId: mockPrincipal,
      date: now - day * BigInt(60),
      weightKg: 82,
      chestCm: 98,
      waistCm: 86,
      hipsCm: 100,
      armsCm: 34,
      legsCm: 56,
      notes: "Starting measurement",
      loggedAt: now - day * BigInt(60),
    },
    {
      id: BigInt(2),
      memberId: mockPrincipal,
      date: now - day * BigInt(30),
      weightKg: 78.5,
      chestCm: 96,
      waistCm: 82,
      hipsCm: 97,
      armsCm: 35,
      legsCm: 57,
      notes: "Mid-point check",
      loggedAt: now - day * BigInt(30),
    },
    {
      id: BigInt(3),
      memberId: mockPrincipal,
      date: now,
      weightKg: 75,
      chestCm: 94,
      waistCm: 78,
      hipsCm: 94,
      armsCm: 36.5,
      legsCm: 58.5,
      notes: "Looking great!",
      loggedAt: now,
    },
  ],
  getMyMemberProfile: async () => ({
    id: mockPrincipal,
    name: "Alex Johnson",
    email: "alex@example.com",
    joinedAt: now - day * BigInt(90),
    assignedMealPlanId: BigInt(1),
  }),
  getMyPlans: async () => [
    {
      memberId: mockPrincipal,
      planId: BigInt(1),
      assignedAt: now - day * BigInt(30),
      expiresAt: now + day * BigInt(60),
    },
  ],
  getSession: async () => ({
    id: BigInt(1),
    title: "HIIT Blast",
    description: "High intensity interval training for maximum caloric burn.",
    startTime: now + day,
    endTime: now + day + BigInt(3_600_000_000_000),
    capacity: BigInt(12),
    bookedCount: BigInt(7),
  }),
  getTransformation: async () => ({
    id: BigInt(1),
    clientName: "Priya Sharma",
    description: "Lost 15kg in 16 weeks following Garima's program.",
    beforeImage: makeBlob("https://placehold.co/400x500/1a2035/ffffff?text=Before"),
    afterImage: makeBlob("https://placehold.co/400x500/0d1b2a/00e5ff?text=After"),
    createdAt: now - day * BigInt(30),
  }),
  getUserProfile: async () => ({ name: "Alex Johnson" }),
  isCallerAdmin: async () => false,
  listAllBookings: async () => [],
  listAllMembers: async () => [
    {
      id: mockPrincipal,
      name: "Alex Johnson",
      email: "alex@example.com",
      joinedAt: now - day * BigInt(90),
      assignedMealPlanId: BigInt(1),
    },
    {
      id: Principal.fromText("2vxsx-fae"),
      name: "Priya Sharma",
      email: "priya@example.com",
      joinedAt: now - day * BigInt(60),
    },
  ],
  listFitnessPlans: async () => [
    {
      id: BigInt(1),
      name: "12-Week Body Transformation",
      description: "Complete body recomposition with progressive overload.",
      durationDays: BigInt(84),
      price: 299,
      createdAt: now - day * BigInt(180),
    },
    {
      id: BigInt(2),
      name: "6-Week Shred",
      description: "Aggressive fat loss program for those ready to commit.",
      durationDays: BigInt(42),
      price: 199,
      createdAt: now - day * BigInt(90),
    },
    {
      id: BigInt(3),
      name: "Muscle Builder Elite",
      description: "Hypertrophy-focused training split with nutrition guidance.",
      durationDays: BigInt(90),
      price: 349,
      createdAt: now - day * BigInt(60),
    },
  ],
  listMealPlans: async () => [
    {
      id: BigInt(1),
      name: "High Protein Fat Loss",
      description: "Calorie deficit plan focused on preserving muscle.",
      goalType: "weightLoss" as unknown as GoalType,
      dailyCalories: BigInt(1800),
      proteinGrams: BigInt(180),
      carbsGrams: BigInt(150),
      fatGrams: BigInt(60),
      createdAt: now,
    },
    {
      id: BigInt(2),
      name: "Muscle Growth Protocol",
      description: "Calorie surplus plan for lean muscle building.",
      goalType: "muscleGain" as unknown as GoalType,
      dailyCalories: BigInt(2800),
      proteinGrams: BigInt(220),
      carbsGrams: BigInt(300),
      fatGrams: BigInt(80),
      createdAt: now,
    },
  ],
  listRecipeGuides: async () => [
    {
      id: BigInt(1),
      title: "High-Protein Breakfast Guide",
      description: "30 quick breakfast recipes rich in protein to fuel your mornings.",
      goalType: "muscleGain" as unknown as GoalType,
      file: makeBlob("https://example.com/recipe-guide-1.pdf"),
      filename: "breakfast-guide.pdf",
      createdAt: now - day * BigInt(14),
    },
    {
      id: BigInt(2),
      title: "Fat-Burning Meal Prep",
      description: "Weekly meal prep plan with calorie-controlled fat-loss recipes.",
      goalType: "weightLoss" as unknown as GoalType,
      file: makeBlob("https://example.com/recipe-guide-2.pdf"),
      filename: "fat-burning-meals.pdf",
      createdAt: now - day * BigInt(7),
    },
  ],
  listSessions: async () => [
    {
      id: BigInt(1),
      title: "HIIT Blast",
      description: "High intensity interval training for maximum caloric burn.",
      startTime: now + day,
      endTime: now + day + BigInt(3_600_000_000_000),
      capacity: BigInt(12),
      bookedCount: BigInt(7),
    },
    {
      id: BigInt(2),
      title: "Strength Foundations",
      description: "Compound lifting session for building total body strength.",
      startTime: now + day * BigInt(2),
      endTime: now + day * BigInt(2) + BigInt(5_400_000_000_000),
      capacity: BigInt(10),
      bookedCount: BigInt(4),
    },
    {
      id: BigInt(3),
      title: "Core & Mobility",
      description: "Deep core work and full-body mobility flow.",
      startTime: now + day * BigInt(3),
      endTime: now + day * BigInt(3) + BigInt(3_600_000_000_000),
      capacity: BigInt(15),
      bookedCount: BigInt(9),
    },
  ],
  listTestimonials: async () => [
    {
      id: BigInt(1),
      clientName: "Garima Makhne",
      content: "Garima's program changed my life. I lost 15kg and gained incredible confidence. Her approach is science-backed and truly transformative.",
      rating: BigInt(5),
      createdAt: now - day * BigInt(45),
    },
    {
      id: BigInt(2),
      clientName: "Denier Hawon",
      content: "Best investment I've made in my health. The nutrition guidance and training plan were perfectly tailored to my goals.",
      rating: BigInt(5),
      createdAt: now - day * BigInt(30),
    },
    {
      id: BigInt(3),
      clientName: "Noch Karacha",
      content: "I was skeptical at first but the results speak for themselves. 12 weeks and I'm a completely different person.",
      rating: BigInt(5),
      createdAt: now - day * BigInt(15),
    },
  ],
  listTransformations: async () => [
    {
      id: BigInt(1),
      clientName: "Priya Sharma",
      description: "Lost 15kg in 16 weeks. Completely transformed her physique and energy levels.",
      beforeImage: makeBlob("https://placehold.co/400x500/1a2035/cccccc?text=Before"),
      afterImage: makeBlob("https://placehold.co/400x500/0d1b2a/00e5ff?text=After"),
      createdAt: now - day * BigInt(30),
    },
    {
      id: BigInt(2),
      clientName: "Rahul Verma",
      description: "Gained 8kg of lean muscle in 12 weeks with the muscle builder program.",
      beforeImage: makeBlob("https://placehold.co/400x500/1a2035/cccccc?text=Before"),
      afterImage: makeBlob("https://placehold.co/400x500/0d1b2a/f59e0b?text=After"),
      createdAt: now - day * BigInt(20),
    },
    {
      id: BigInt(3),
      clientName: "Ananya Singh",
      description: "Complete body recomposition — lost fat and gained definition over 20 weeks.",
      beforeImage: makeBlob("https://placehold.co/400x500/1a2035/cccccc?text=Before"),
      afterImage: makeBlob("https://placehold.co/400x500/0d1b2a/00e5ff?text=After"),
      createdAt: now - day * BigInt(10),
    },
  ],
  logMacros: async (date, calories, proteinGrams, carbsGrams, fatGrams, notes) => ({
    id: BigInt(Date.now()),
    memberId: mockPrincipal,
    date,
    calories,
    proteinGrams,
    carbsGrams,
    fatGrams,
    notes,
    loggedAt: now,
  }),
  logMeasurement: async (date, weightKg, chestCm, waistCm, hipsCm, armsCm, legsCm, notes) => ({
    id: BigInt(Date.now()),
    memberId: mockPrincipal,
    date,
    weightKg,
    chestCm,
    waistCm,
    hipsCm,
    armsCm,
    legsCm,
    notes,
    loggedAt: now,
  }),
  saveCallerUserProfile: async () => {},
  saveMemberProfile: async () => {},
  _immutableObjectStorageBlobsAreLive: async () => [],
  _immutableObjectStorageBlobsToDelete: async () => [],
  _immutableObjectStorageConfirmBlobDeletion: async () => {},
  _immutableObjectStorageCreateCertificate: async () => ({} as never),
  _immutableObjectStorageRefillCashier: async () => ({} as never),
  _immutableObjectStorageUpdateGatewayPrincipals: async () => {},
  _initializeAccessControl: async () => {},
};
